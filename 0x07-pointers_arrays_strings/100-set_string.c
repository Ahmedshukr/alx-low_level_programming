#include "main.h"

/**
 * set_string - Entry point
 * @s: a pointer to pointer of character
 * @to: a pointer to to
 * Return: Always 0 (Success)
 */

void set_string(char **s, char *to)
{
	*s = to;
}
